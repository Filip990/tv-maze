import styled from "styled-components";
// will be used once I Make pagination a separate component
export const StyledPagination = styled.ul`
  list-style: none;
  display: block;
`;

export const ActiveButton = styled.button`
  background: blue;
  color: white;
`;
